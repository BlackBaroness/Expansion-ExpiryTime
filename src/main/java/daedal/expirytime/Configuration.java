package daedal.expirytime;

public record Configuration(
        String numbersColor,
        String never,
        String days,
        String hours,
        String minutes,
        String seconds
) {
}
